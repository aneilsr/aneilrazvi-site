# OS + tooling
.DS_Store
.vercel/
node_modules/

# Local-only masters and scratch (kept on disk, not in the repo)
_source/
_to_delete/
_pre-sweep-backup/
_backup/
Photos - to pull from/
*.zip

# Anything prefixed with an underscore is local-only by convention.
# Backup folders MUST never ship: Vercel serves every file in the repo,
# so a committed backup republishes whatever you just removed.
_*/
